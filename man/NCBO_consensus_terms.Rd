% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rncbo_dictionary.R
\name{NCBO_consensus_terms}
\alias{NCBO_consensus_terms}
\title{NCBO_consensus_terms}
\usage{
NCBO_consensus_terms(
  terms,
  ncbo_results_path,
  terms_alternate_names_df = NULL,
  type = NULL,
  main_category = NULL,
  q = 5,
  dist = 0.15
)
}
\arguments{
\item{terms}{\code{character}. The file path to a single-column CSV containing the
initial list of terms to process.}

\item{ncbo_results_path}{\code{character}. The file path to the CSV file containing
the ontology mapping results generated by the \code{NCBO_mapper} function.}

\item{terms_alternate_names_df}{\code{data.frame}. An optional data frame
with two columns: \code{terms} and \code{alternate_names}. This is used to supplement
the synonym generation with manually curated alternatives. Default is \code{NULL}.}

\item{type}{\code{character}. The PICO element type to assign to all
terms in the dictionary (e.g., "Outcome", "Intervention"). Default is \code{NULL}.}

\item{main_category}{\code{character}. A broad category to assign to all
terms, describing the general domain (e.g., "Sample", "Biomarker"). Default is \code{NULL}.}

\item{q}{\code{numeric}. The q-gram size used for the cosine similarity calculation
during the deduplication step (compare list of alternate_names). Default is 5.}

\item{dist}{\code{numeric}. The distance used for the cosine similarity calculation
during the deduplication step (compare list of alternate_names). Default is 0.15.}
}
\value{
A \code{tibble} representing the final PICO dictionary. The data frame has
the following columns:
\itemize{
\item \code{name}: The chosen primary name for the term/concept.
\item \code{alternate_names}: A pipe-separated string of synonyms.
\item \code{type}: The PICO type (e.g., "Outcome").
\item \code{main_category}: The main domain category (e.g., "Biomarker").
\item \code{sub_category1}: The most common parent concept from NCBO.
\item \code{sub_category2}: The second most common parent concept.
\item \code{regex}: An empty column, intended as a placeholder for regex patterns.
}
}
\description{
This function orchestrates a multi-step process to generate a clean,
deduplicated PICO (Population, Intervention, Comparison, Outcome) dictionary.
It starts with a raw list of terms, enriches them with synonyms and parent
concepts from NCBO BioPortal results, and refines them through clustering
and similarity-based merging.
}
\details{
The process involves the key stages:
\enumerate{
\item \strong{Hierarchical Clustering:} Groups similar terms together based on
Jaro-Winkler string distance to form initial synonym clusters.
\item \strong{Enrichment:} Merges user-provided alternate names and synonyms/parents
from the NCBO mapping results.
\item \strong{Normalization & Summarization:} Cleans and aggregates the data,
consolidating synonyms and extracting the top parent concepts for each term.
\item \strong{Deduplication:} Iteratively compares synonym lists using cosine
similarity to find and merge highly similar entries, ensuring each
conceptual entity is represented only once.
\item \strong{Finalization:} Constructs the final PICO dictionary with a standardized
set of columns (solesR).
}
}
