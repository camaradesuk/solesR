% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pico_tag.R
\name{pico_retag}
\alias{pico_retag}
\title{Pico Retag}
\usage{
pico_retag(con, tag_type, tag_method = "both")
}
\arguments{
\item{con}{a database connection}

\item{tag_type}{A string specifying the PICO tag type to re-tag. Must match a value in the
\code{type} column of the \code{pico_ontology} database table.}

\item{tag_method}{A string specifying the tagging method to use. Options are:
\itemize{
\item \code{"both"} — re-tag using both \code{tiabkw} and \code{fulltext}.
\item \code{"tiabkw"} — re-tag using title/abstract keywords.
\item \code{"fulltext"} — re-tag using full text.
}
Default is \code{"both"}.}
}
\value{
The function updates the \code{pico_tag} table in the database.
}
\description{
A function to update your pico tags when changes to the pico dictionary have been made. This function
uses the \code{complete_pico_tag()} and may take a long time to run. It is therefore recommended to schedule
it as a background process.
}
\examples{
\dontrun{
# Re-tag interventions using both tiabkw and fulltext
pico_retag(con, tag_type = "intervention", tag_method = "both")

# Re-tag outcomes using only tiabkw
pico_retag(con, tag_type = "outcome", tag_method = "tiabkw")
}
}
