% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_api.R
\name{safe_col}
\alias{safe_col}
\title{Safely extract a (possibly nested) column from a data frame}
\usage{
safe_col(df, col, subcol = NULL, sub2col = NULL, default = NA_character_)
}
\arguments{
\item{df}{a data frame}

\item{col}{character string, the top-level column name to extract}

\item{subcol}{optional character string, a sub-field to extract from a
nested data frame column (e.g. \code{"source"} -> \code{"pages"})}

\item{sub2col}{optional character string, a further nested sub-field
within \code{subcol} (e.g. \code{"source"} -> \code{"pages"} ->
\code{"range"}). Ignored if \code{subcol} is \code{NULL}.}

\item{default}{value to fill with if the requested column/sub-field is
missing (default \code{NA_character_})}
}
\value{
a vector of length \code{nrow(df)}
}
\description{
Returns a vector for the requested column from \code{df}, defaulting to
a vector of \code{NA} (of length \code{nrow(df)}) if the column, or a
nested sub-field (up to two levels deep), does not exist. This guards
against the WoS Starter API omitting fields for some records.
}
